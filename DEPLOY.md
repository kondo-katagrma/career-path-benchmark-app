# URL入力を使うための設置手順（Apps Script Webアプリ）

このアプリで「**スプレッドシートのURLを貼るだけ**」で取り込むには、カタグルマのGoogleアカウントに小さなWebアプリを**1回だけ**設置します。集計はGoogle側で行い、**集計値（数値）のみ**を返すため、氏名などの個人情報（PII）はGoogleの外（ブラウザ）に出ません。

## 手順（管理者が一度だけ）
1. カタグルマのアカウントで <https://script.google.com> を開く。
2. **「新しいプロジェクト」** を作成。
3. 同梱の **`AppsScript_Code.gs`** の中身を全部貼り付けて保存。
4. （推奨）簡易トークンを設定：**プロジェクトの設定（歯車）→ スクリプト プロパティ → プロパティを追加** → 名前 `TOKEN`／値は任意の文字列。
5. 右上 **「デプロイ」→「新しいデプロイ」→ 種類「ウェブアプリ」**。
   - **実行するユーザー**：自分（カタグルマのアカウント）
   - **アクセスできるユーザー**：全員（匿名を含む）　※ブラウザから取得できるようにするため。上のトークンで簡易保護します。
6. 表示される **ウェブアプリのURL**（`https://script.google.com/macros/s/……/exec`）をコピー。
7. **エディタで `setupBaseline` 関数を1回だけ実行**（画面上部の関数選択で `setupBaseline` を選び「実行」）。
   - 初回は「承認が必要です」と出るので、**Googleドライブへの読み取りアクセスを許可**する（マスターフォルダ配下の全法人フォルダを走査して全法人ベースラインを作るため）。
   - 実行ログに `セットアップ完了: lawCount=… peopleCount=…` と出れば成功。同時に、**毎日3時（JST）に自動でベースラインを更新するトリガー**も設定される。
8. 各利用者のアプリに、このURL（とトークン）を設定する。方法はどちらか：
   - **A（配布が楽）**：配る前に `index.html` の `WEBAPP_URL` と `WEBAPP_TOKEN` を書き換えて配布 → メンバーは設定不要。
   - **B（各自設定）**：メンバーが index.html を開き、ブラウザのコンソールで一度だけ実行：
     ```js
     localStorage.setItem('cpb_webapp_url','＜コピーしたURL＞');
     localStorage.setItem('cpb_webapp_token','＜手順4のTOKEN。設定した場合＞');
     ```
9. 完了。アプリでスプレッドシートのURLを貼り「**URLから取得**」を押すと、傾向表が出ます。

## 全法人ベースラインの仕組み（自動更新）
- ベースライン（全法人の集計値）は `index.html` に埋め込まれた静的データではなく、**マスターフォルダ**（`AppsScript_Code.gs` 内 `MASTER_FOLDER_ID`）配下の法人フォルダを毎日自動で走査して作り直される。新しい案件フォルダが増えると、翌日の自動更新から自動的に反映される。
- **除外の仕組み**：
  - フォルダ名の部分一致で常に除外したい法人があれば、スクリプトプロパティ `BASELINE_EXCLUDE_NAMES` にカンマ区切りで設定する（例：`バルツァ,芦屋`）。進行中で回答が固まっていない案件など。
  - **分析対象の法人自身は、URL貼り付け時に自動でベースラインから除外される**（対象スプレッドシートが属するフォルダを特定し、その場で除外して再計算するため、自己比較にならない）。この場合は取得に30秒前後かかる（キャッシュを使わず都度計算するため）。
  - ファイル選択（.xlsx/.csv）で分析する場合は、Drive上のファイルと紐づかないため自己除外はできず、キャッシュ済みの全法人版（対象法人を含む可能性あり）が使われる。
- 手動で今すぐ更新したい場合は、エディタで `refreshBaselineCache` を実行する。
- 回答スプレッドシートの自動判定は、ファイル名に「大元・商談用・保存用」を含むものだけを除外し、それ以外のGoogleスプレッドシートのタブを「役職」「勤続」列の有無で判定する（法人ごとの「〇〇御中】キャリアパス策定支援フォーマット」といった命名は除外されない）。

## セキュリティ・PII
- Webアプリは **集計値（数値）だけ** を返します。氏名・回答本文などのPIIはGoogle側で処理し、外部には出しません。
- 集計できるのは **実行アカウント（カタグルマ）が閲覧できるスプレッドシート** のみ（対象は共有 or 同一アカウント所有である必要があります）。ベースライン計算も同様に、実行アカウントがアクセスできるマスターフォルダ配下のみが対象です。
- `TOKEN` を設定すると、URLを知っていても一致しなければ使えません。

## うまくいかないとき
- 「未設定」と出る → 手順8でURLが設定できていない。
- 「認証エラー」→ トークン不一致（手順4と8を合わせる）。
- 「回答シートが見つかりません」→ そのスプレッドシートに、役職・勤続などの列を持つ回答タブが無い。
- 「通信に失敗」→ デプロイのアクセス範囲が「全員」になっているか、URL末尾が `/exec` か確認。
- 全法人の法人数・人数が想定より少ない → その法人のフォルダ内に、回答タブ（役職・勤続の列を持つ）が見つかっていない可能性がある。フォルダ構成やタブの見出しを確認し、必要なら `AppsScript_Code.gs` の `findResponseRowsInFolder` の探索範囲（現在はサブフォルダ1階層まで）を見直す。
